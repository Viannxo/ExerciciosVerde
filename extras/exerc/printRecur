#include <stdio.h>
void impri(int, int);

int main (){
    int n;
    printf("digite valor de n: \n");
    scanf("%d", &n);
    impri(n, 1);
    return 0;
}

void impri(int n, int x){
    if(n==0){
        return;
    }
    printf(" %d ", x);
    impri(n-1, x+1);
    printf(" %d ", x);
}
